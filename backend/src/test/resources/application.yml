spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:taskmanager-test;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: TestOnlyJwtSecretKeyThatIsLongEnoughForHs256Algorithms123456
    expiration-ms: 3600000
